#!/bin/bash

cd .dotfiles
stow X
stow bash
stow compton
stow tmux
stow rofi
stow scripts
stow xmonad
cd ..

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi
